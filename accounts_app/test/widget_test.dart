import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder: app requires async init with SharedPreferences and Hive
    expect(true, isTrue);
  });
}
